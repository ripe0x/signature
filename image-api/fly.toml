app = "fold-image-api"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"
  CACHE_ENABLED = "true"
  CHAIN = "mainnet"
  CONTRACT_ADDRESS = "0x008B66385ed2346E6895031E250B2ac8dc14605C"
  # RPC_URL must be set as a secret: fly secrets set RPC_URL="https://..."

[http_service]
  internal_port = 3001
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[mounts]
  source = "image_cache"
  destination = "/data/cache"

[[vm]]
  memory = "2gb"
  cpu_kind = "shared"
  cpus = 1
